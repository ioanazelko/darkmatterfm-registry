# Capability-gap development roadmap (item #9, application A7)

Built from the full run: **2,004 annotated model cards, 5166 capability-gap entries** in `registry/models/capability_gaps_full.jsonl`, grouped into **150 (gap type, capability theme) groups** and weighted by the summed `adjusted_priority_score` of the models each group blocks (from `ranking_full_ranked.jsonl`).

A *theme* is the normalized capability behind many model-specific gap descriptions (e.g. every "no Lyman-α likelihood for this model's transfer function" lands in the Lyman-α theme). The browsable version with every member model is `registry/gap_roadmap.html`; machine-readable groups are in `registry/models/capability_gap_roadmap.jsonl`.

## Top gaps overall

### Relic-abundance solver extension (micrOMEGAs/MadDM/DarkSUSY)  —  `tool`
Priority weight 11,344 across 470 models (480 gap entries overall). Severity: blocks_all_observables × 33, blocks_key_observables × 287, degrades_precision × 138, minor × 22. Effort: medium × 304, large × 25, small × 151.

- **Cosmic-ray boosted sub-GeV gravitationally interacting dark matter with a spin-2 mediator** ([arXiv:1912.09904](https://arxiv.org/abs/1912.09904), score 48): The momentum-dependent spin-2 (KK graviton) CR-DM scattering cross section and boosted-flux calculation exist only as a private modification of DarkSUSY; no public code computes dPhi_chi/dT_chi and its Earth attenuation for the G^{mu nu}T_{mu nu} coupling with its strong q^2 dependence.
  *Proposed resolution:* Extend the public DarkSUSY CRDM module with the spin-2 mediator differential cross section d(sigma)/dT for CR protons/helium on chi (parameterized by m_chi, m_G, c_SM/Lambda, c_DM/Lambda) and thread the same momentum dependence through the layered-Earth attenuation solver, validating against the paper's O(1e-35) cm^2 benchmark reach.
- **Inelastic dark matter with a dark photon mediator** ([arXiv:1911.03176](https://arxiv.org/abs/1911.03176), score 46): The FeynRules/CalcHEP model file for the two-state Majorana inelastic dark matter with kinetically mixed dark photon is not confirmed public, blocking off-the-shelf reproduction of the micrOMEGAs coannihilation thermal targets over (m_chi1, Delta, m_A', epsilon, alpha_D).
  *Proposed resolution:* Release or re-derive the FeynRules v2.3.32 iDM implementation, export CalcHEP files for micrOMEGAs v5.0.6, and validate against the paper's benchmark thermal targets at m_A'/m_chi1 = 2.5 and 3 with Delta = 0.1 m_chi1.
- **PQ Dirac-neutrino model with scalar WIMP and axion dark matter** ([arXiv:2001.01846](https://arxiv.org/abs/2001.01846), score 42): No public micrOMEGAs/CalcHEP (or equivalent) implementation of the mixed singlet-doublet scalar sector of this one-loop PQ Dirac-neutrino model exists, so the scalar WIMP freeze-out and sigma_SI cannot be computed beyond the paper's limiting-case benchmarks.
  *Proposed resolution:* Implement the eta_u-singlet scalar sector (mass eigenstates, quartic couplings, Higgs and gauge interactions) in FeynRules, export to CalcHEP, and run micrOMEGAs for Omega_S h^2 and sigma_SI across the singlet-doublet mixing plane.
- … and 477 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `tool`
Priority weight 9,623 across 367 models (374 gap entries overall). Severity: blocks_all_observables × 33, blocks_key_observables × 281, degrades_precision × 54, minor × 6. Effort: small × 19, medium × 322, large × 33.

- **Decaying warm majoron dark matter** ([arXiv:1803.05650](https://arxiv.org/abs/1803.05650), score 52): No public Boltzmann-solver setup treats a keV-scale warm majoron with its thermal free-streaming distribution that also decays to relativistic neutrinos with tau_J of order 50 Gyr; the CLASS decaying-CDM module assumes cold parents, so the combined warm-plus-decaying linear evolution (T(k) and CMB spectra) cannot currently be computed.
  *Proposed resolution:* Extend CLASS by combining its non-cold-dark-matter (ncdm) sector with the decaying-dark-matter module: give the ncdm species a decay term sourcing massless neutrino dark radiation, parameterized by (m_J, tau_J, initial phase-space distribution), and validate against the paper's N-body initial conditions and the tau_J > 50 Gyr CMB benchmark.
- **Scalar dark matter with a long-range dark fifth force** ([arXiv:2204.08484](https://arxiv.org/abs/2204.08484), score 46): The modified CLASS implementing the dark fifth-force background (H(z), rho_dm(z), s(z)) and linear perturbations (relative DM-baryon density and velocity modes) is not publicly released, so the whole CMB+BAO pipeline cannot be rerun or extended by others.
  *Proposed resolution:* Obtain or reimplement the fifth-force patch in public CLASS: add the scalar-field background with m_chi(s) coupling and beta, m_s/H_0 parameters plus the coupled DM-baryon perturbation equations, and validate against the published beta < 0.011 (Planck) and beta < 0.0054 (Planck+BAO) bounds.
- **Non-canonical asymptotically-flat coth-potential unified dark sector** ([arXiv:1803.09767](https://arxiv.org/abs/1803.09767), score 46): No Boltzmann-code implementation exists for the non-canonical UDM scalar with L=X(X/M^4)^(alpha-1)-V(phi) and the asymptotically-flat coth potential: neither CLASS nor CAMB evolves the perturbations of a single field whose kinetic energy clusters as dark matter with c_s^2=(2 alpha-1)^-1 while its potential drives acceleration, so the transfer function and Jeans-scale viability test cannot be computed.
  *Proposed resolution:* Extend CLASS with a k-essence/UDM module that takes the background solution for this potential and evolves the exact scalar perturbation equations (or, as a first pass, map the model onto CLASS's generalized-dark-matter fluid with w(a) from the background solve and constant c_s^2=(2 alpha-1)^-1), validating the pressureless large-alpha limit against the paper.
- … and 371 more models (see `gap_roadmap.html`).

### Indirect-detection likelihood (γ-ray / cosmic-ray / ν)  —  `likelihood`
Priority weight 8,094 across 333 models (343 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 159, degrades_precision × 162, minor × 21. Effort: medium × 258, small × 56, large × 29.

- **Baryon-triality DFSZ axion and light-axino dark matter model** ([arXiv:1807.02530](https://arxiv.org/abs/1807.02530), score 45): No public likelihood exists for X-ray/gamma-ray line limits on a decaying keV axino that is only a fraction of the dark matter; the card notes limits must be rescaled by the axino fraction and records likelihood_available: no for con_axino_xray_gamma.
  *Proposed resolution:* Digitize published X-ray line (e.g. NuSTAR/XMM blank-sky) and soft gamma-ray decaying-DM lifetime curves, wrap them as a lifetime-versus-mass likelihood with an explicit Omega_axino/Omega_DM rescaling parameter, and connect it to the analytic (m_axino, lambda, lambda_prime, f_a) to lifetime map.
- **Right-handed neutrino hot dark matter subcomponent in an SU(2)_V W-prime model** ([arXiv:1804.04135](https://arxiv.org/abs/1804.04135), score 44): No numerical flavor/EWP/collider recast exists for the W-prime SU(2)_V sector that fixes the right-handed-neutrino parameters; the R_D(*), FCNC, EWP, and direct-search constraints are only described qualitatively (likelihood_available 'no', recasting_needed 'yes').
  *Proposed resolution:* Encode the C_RR^V operator and right-handed mixing structure in a global flavor-fit package (e.g. extend flavio/smelli with the light-nu_R final state) and recast LHC W-prime and vector-like fermion searches for the model's benchmark spectra.
- **Thermal dark matter freeze-out during kination** ([arXiv:2304.12336](https://arxiv.org/abs/2304.12336), score 40): No packaged likelihood pipeline connects the kination-enhanced microhalo boost factor to Fermi-LAT dwarf-spheroidal, H.E.S.S. Galactic Center, and isotropic gamma-ray background limits; the constraint currently requires a manual channel-by-channel recast through the boost factor.
  *Proposed resolution:* Wire the microhalo-models boost output into the released Fermi-LAT dwarf-spheroidal bin-by-bin likelihoods and published H.E.S.S. limit curves, with annihilation spectra per channel from PPPC4DMID or HDMSpectra, exposing (m_DM, <sigma v>, T_KR, T_kd, channel) as inputs.
- … and 340 more models (see `gap_roadmap.html`).

### Lyman-α forest modeling & likelihood  —  `likelihood`
Priority weight 7,940 across 279 models (280 gap entries overall). Severity: blocks_key_observables × 132, degrades_precision × 147, minor × 1. Effort: medium × 223, large × 50, small × 7.

- **Nonthermal sub-keV light dark matter fraction from radiative decays** ([arXiv:1912.05563](https://arxiv.org/abs/1912.05563), score 52): The structure-formation bound f < 0.01 is inferred from generic mixed cold/hot arguments rather than from a dedicated likelihood: no Lyman-alpha, CMB, or sigma8 likelihood has been run against this model's boosted-subcomponent transfer function, so the actual upper limit on f versus m_DM and tau is uncalibrated.
  *Proposed resolution:* Recast an existing Lyman-alpha forest likelihood or emulator (e.g. the hydrodynamical-simulation-calibrated WDM/mixed-DM chains) plus Planck CMB data onto the mixed transfer functions from gap_2, producing f(m_DM, tau) exclusion contours.
- **Dark-fermion-production relaxion warm dark matter** ([arXiv:1909.07706](https://arxiv.org/abs/1909.07706), score 48): No public Lyman-alpha likelihood applies to this model's mixed case: the constraint is imposed only as the thermal-relic 5-5.3 keV m_WDM bound, which does not cover a partial warm fraction f_WDM < 1 or a diluted non-standard spectrum.
  *Proposed resolution:* Recast the model through an existing Lyman-alpha flux-power emulator/likelihood built for mixed cold-plus-warm dark matter, parameterized by (m_WDM_equivalent, f_WDM), using the transfer functions from the derived f(p) rather than the pure-thermal mapping.
- **Neutrino portal freeze-in dark matter** ([arXiv:1806.08579](https://arxiv.org/abs/1806.08579), score 45): No ready Lyman-alpha likelihood accepts the nonthermal freeze-in momentum distribution directly; the card notes the thermal-WDM template can misestimate the bound, and the exact transfer-function-to-likelihood mapping was not reproduced.
  *Proposed resolution:* Run the tabulated f(p) through CLASS ncdm to obtain T(k), then apply a shape-based Lyman-alpha recast (area criterion or the Murgia et al. alpha-beta-gamma parameterization with the MIKE/HIRES+XQ-100 likelihood) instead of the half-mode thermal-WDM mapping.
- … and 277 more models (see `gap_roadmap.html`).

### Indirect-detection likelihood (γ-ray / cosmic-ray / ν)  —  `tool`
Priority weight 5,819 across 243 models (247 gap entries overall). Severity: blocks_all_observables × 15, blocks_key_observables × 164, degrades_precision × 55, minor × 13. Effort: medium × 157, small × 72, large × 18.

- **Gauged B-L Dirac-neutrino two-component fermion dark matter** ([arXiv:1911.04703](https://arxiv.org/abs/1911.04703), score 48): No public model implementation exists for this anomaly-free B-L variant: the fractional-charge chiral fermion sector, extended scalar potential, and two-component (psi_1, psi_2) coupled freeze-out with conversion annihilation must be re-encoded before micrOMEGAs can reproduce the paper's relic and direct-detection numbers.
  *Proposed resolution:* Implement the charge assignments, scalar sector, and Yukawa structure in FeynRules, export CalcHEP files, and run micrOMEGAs 5 in two-DM-component mode including psi_2 psi_2 -> psi_1 psi_1 conversion, validating against the paper's Omega_psi1 + Omega_psi2 = 0.120 benchmark regions.
- **UV freeze-in baryogenesis with Dirac fermion dark matter** ([arXiv:2204.13554](https://arxiv.org/abs/2204.13554), score 41): No public code solves the coupled freeze-in plus CP-violating scattering asymmetry Boltzmann equations for the dimension-6 Dirac-fermion model with vector-like leptons; the paper's numerical scans are custom and unreleased, so Y_B and the viable Lambda-T_RH band cannot be recomputed.
  *Proposed resolution:* Implement the paper's coupled yield and B-L asymmetry equations (dimension-6 collision terms, tree-loop interference CP source, sphaleron conversion) in a released standalone solver, e.g. a Python/ULYSSES-style momentum-averaged Boltzmann code validated against the paper's benchmark Lambda-T_RH scaling.
- **331 WIMPy dark radiation model** ([arXiv:1308.6630](https://arxiv.org/abs/1308.6630), score 40): No public micrOMEGAs/CalcHEP model file for the 3-3-1LHN scalar WIMP sector is identified in the card, so the thermal relic input to the route cannot be reproduced without re-implementing the model.
  *Proposed resolution:* Implement the 3-3-1LHN Lagrangian (scalar WIMP, N_R fermions, 3-3-1 gauge and scalar sectors) in FeynRules/SARAH and export a CalcHEP model file for micrOMEGAs freeze-out computation.
- … and 244 more models (see `gap_roadmap.html`).

### Relic-abundance solver extension (micrOMEGAs/MadDM/DarkSUSY)  —  `likelihood`
Priority weight 3,944 across 165 models (168 gap entries overall). Severity: blocks_key_observables × 34, degrades_precision × 129, minor × 5. Effort: medium × 79, small × 86, large × 3.

- **Resonantly produced sterile neutrino dark matter in non-standard pre-BBN cosmology** ([arXiv:1911.03398](https://arxiv.org/abs/1911.03398), score 41): X-ray line and DEBRA diffuse-background decay limits are available only as published exclusion curves for the standard-cosmology relic density; no packaged likelihood exists that rescales the bounds by the model's cosmology-dependent sterile density Omega_s/Omega_DM across targets.
  *Proposed resolution:* Assemble published X-ray (galaxy, cluster, blank-sky) and DEBRA limits into a single interpolable likelihood in (m_s, sin^2(2theta)) with an explicit linear rescaling by the produced density fraction, documenting target column-density assumptions so bounds update consistently as production changes.
- **Electrically charged massive particle dark matter** ([arXiv:1604.07926](https://arxiv.org/abs/1604.07926), score 39): Terrestrial, heavy-isotope, cosmic-ray, and millicharged-particle bounds on charged relics are only summarized from prior literature; no reusable likelihood or recast exists, and the cosmic-ray bounds rest on Galactic-inflow and magnetic-expulsion assumptions that are not modeled.
  *Proposed resolution:* Derive a recast module mapping (m_Ch, q_Ch, local CHAMP flux) to heavy-isotope search and cosmic-ray detector exclusion curves, explicitly parameterizing the Galactic magnetic-expulsion suppression of the local charged-relic density, and publish it as tabulated exclusion likelihoods.
- **WIMPflation stable inflaton dark matter** ([arXiv:1807.03308](https://arxiv.org/abs/1807.03308), score 38): No assembled joint likelihood connects the shared parameter space to both Planck slow-roll observables and xenon direct-detection limits; each constraint is only quoted as literature exclusion statements (likelihood_available: no on all three constraints).
  *Proposed resolution:* Implement the Higgs-portal scalar in DDCalc/micrOMEGAs for sigma_SI limits and combine with the Planck n_s-r contours in a single scan over (potential parameters, m_phi, lambda_phiH), reproducing the quoted m_phi > 900 GeV bound as a validation test.
- … and 165 more models (see `gap_roadmap.html`).

### Direct-detection recast & likelihood  —  `likelihood`
Priority weight 3,868 across 168 models (169 gap entries overall). Severity: blocks_key_observables × 50, degrades_precision × 111, minor × 8. Effort: medium × 76, small × 91, large × 2.

- **Cosmic-ray boosted sub-GeV gravitationally interacting dark matter with a spin-2 mediator** ([arXiv:1912.09904](https://arxiv.org/abs/1912.09904), score 48): No proper experimental likelihood is implemented for the boosted-DM recoil spectrum: the XENON1T constraint is an approximate bin-by-bin recast without official detector response/efficiency tables, and no electron-recoil (S2-only) likelihood recast exists for the spin-2 model at all.
  *Proposed resolution:* Build a binned-count likelihood from XENON1T (and current LZ/XENONnT) public data releases with published efficiency curves, and add an S2-only ionization-channel recast mapping the boosted spectrum through the xenon charge-yield model, so exclusions become reproducible statistical limits rather than order-of-magnitude reaches.
- **Decaying warm dark matter with a massive dark-photon daughter** ([arXiv:2006.12348](https://arxiv.org/abs/2006.12348), score 38): The xenon absorption-rate step lacks a detector-response model and a packaged XENON1T electronic-recoil likelihood, so the 2-3 keV dark-photon absorption window is fit only at the level of quoted source regions.
  *Proposed resolution:* Combine public xenon photoelectric cross-section tables with the XENON1T energy resolution, efficiency, and background model from the electronic-recoil data release into a binned likelihood for the (m_Aprime, epsilon, dark-photon fraction) absorption line.
- **Minimal nonminimal-coupling inflaton dark matter** ([arXiv:2205.02906](https://arxiv.org/abs/2205.02906), score 38): The direct-detection constraint is only the XENON1T-derived coupling bound lambda_hphi(1 TeV) < 0.25 quoted in the text; no current-generation (LZ/XENONnT) likelihood is implemented for the Higgs-portal scalar near and away from the resonance window.
  *Proposed resolution:* Implement the scalar-singlet Higgs-portal cross section in DDCalc (or reuse existing singlet-scalar global-fit likelihoods, e.g. GAMBIT's) with current LZ/XENONnT data and combine with the analytic RG-unitarity consistency condition.
- … and 166 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `likelihood`
Priority weight 3,690 across 139 models (140 gap entries overall). Severity: blocks_key_observables × 49, degrades_precision × 86, minor × 5. Effort: medium × 96, small × 37, large × 7.

- **Decaying warm majoron dark matter** ([arXiv:1803.05650](https://arxiv.org/abs/1803.05650), score 52): The Ly-alpha forest constraint is available only as a 5.3 keV thermal-relic-WDM lower bound (XQ-100 plus HIRES/MIKE); no public flux-power likelihood or emulator accepts the decaying-warm-majoron transfer function directly, so applying the bound requires an ad hoc thermal-equivalent mapping that the card flags as approximate.
  *Proposed resolution:* Recast via a general small-scale-suppression Ly-alpha emulator: evaluate the majoron T(k) from the extended Boltzmann code and pass it through an existing Ly-alpha flux-power emulator framework trained on generalized cutoff shapes (alpha-beta-gamma parameterization), calibrating any decay-induced late-time suppression with a small set of dedicated hydrodynamic or N-body runs.
- **Gauged B-L Dirac-neutrino two-component fermion dark matter** ([arXiv:1911.04703](https://arxiv.org/abs/1911.04703), score 48): The Delta N_eff constraint is applied only as the quoted static bound Delta N_eff <= 0.285 with an analytic decoupling estimate; no pipeline folds the g_BL- and M_ZBL-dependent right-handed-neutrino decoupling into a current CMB+BBN likelihood, which the card flags as a leading constraint that was not updated.
  *Proposed resolution:* Compute T_dec(g_BL, M_ZBL) with momentum-averaged collision rates, map to Delta N_eff via the entropy-dilution relation, and evaluate against the Planck 2018 + BAO (and forecast CMB-S4) N_eff posterior in CLASS/MontePython to replace the fixed 0.285 cut.
- **Non-canonical asymptotically-flat coth-potential unified dark sector** ([arXiv:1803.09767](https://arxiv.org/abs/1803.09767), score 46): No data-comparison pipeline exists for this potential: the paper is a background demonstration with no likelihood table, and no implemented analysis confronts H(z), distances, and P(k) of the coth-potential UDM with CMB, BAO, SN, and LSS data.
  *Proposed resolution:* Once the perturbation module exists, wrap the model as a Cobaya theory class and run it against Planck 2018, BAO, and Pantheon+ likelihoods plus a linear P(k)/S8 probe to obtain posteriors on (alpha, M, V0, initial conditions).
- … and 137 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `tool`
Priority weight 3,624 across 134 models (134 gap entries overall). Severity: blocks_all_observables × 10, blocks_key_observables × 87, degrades_precision × 34, minor × 3. Effort: medium × 94, large × 28, small × 12.

- **Fuzzy dark matter with repulsive quartic self-interaction** ([arXiv:1805.08112](https://arxiv.org/abs/1805.08112), score 46): The modified CLASS module implementing the quartic-FDM effective-fluid sound speed c_s^2(a,k) and the quartic-dominated (radiation-like) background phase is not publicly released; without it neither the matter-power cutoff nor the equality-shift observable can be recomputed or the lambda(m) bound updated with current data.
  *Proposed resolution:* Reimplement the paper's averaged scalar-fluid treatment as a public CLASS extension (natural host: extend AxiCLASS's ultralight-axion fluid module with the lambda phi^4 correction to w(a) and c_s^2(a,k)), validate against the paper's quoted log10(lambda) = -91.86 + 4 log10(m/1e-22 eV) exclusion line, and couple it to MontePython/Cobaya with current Planck and LSS likelihoods.
- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): No detector-level signal model exists for the millicharged-pion and pi0-photon couplings; the paper defers resonant-detector and modified-electrodynamics forecasts to future work, so laboratory observability cannot be evaluated.
  *Proposed resolution:* Derive the g_pi_gamma and millicharge signal power in axion-haloscope and light-shining-through-wall setups, and recast existing haloscope/millicharge exclusion data onto the (epsilon, m_pi, F_pi) plane.
- **Extreme axion dark matter with cosine potential fluid modeling** ([arXiv:2311.02052](https://arxiv.org/abs/2311.02052), score 42): The modified axionCAMB implementing the full cosine potential, extreme hilltop initial angles, the effective-sound-speed fluid switch, and the background lookup-table acceleration is described in the paper but not publicly released, so no one else can compute CMB or matter power spectra for extreme axions.
  *Proposed resolution:* Obtain or reimplement the cosine-potential fluid treatment as a public axionCAMB (or CLASS) patch, including the shooting solver for f_a given (m_a, theta_i, Omega_a h^2) and validation against the paper's field-equation comparisons.
- … and 131 more models (see `gap_roadmap.html`).

### Other / unclassified  —  `likelihood`
Priority weight 3,577 across 139 models (140 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 54, degrades_precision × 70, minor × 15. Effort: small × 42, medium × 93, large × 5.

- **Line-motivated resonantly produced sterile neutrino N1 dark matter** ([arXiv:2303.15513](https://arxiv.org/abs/2303.15513), score 40): No unified public X-ray likelihood exists for the 7.1 keV benchmark that jointly treats the reported 3.55 keV detections (clusters, M31, Milky Way) and the strongest non-detection limits with consistent instrument responses and halo D-factors; the card records a disputed constraint landscape with only partial likelihood availability.
  *Proposed resolution:* Build a joint spectral likelihood over the archival XMM/Chandra/Hitomi datasets (and XRISM velocity-resolved spectra as they arrive) with shared nuisance parameters for halo profiles and instrumental lines, released as an XSPEC/3ML-compatible module returning constraints in the (m_s, sin^2 2theta) plane.
- **Axi-Higgs ultralight axion cosmology** ([arXiv:2111.01347](https://arxiv.org/abs/2111.01347), score 39): No likelihood exists that confronts the axi-Higgs late-time Higgs-VEV relaxation history v(t)/v0, parameterized by (m_a, C, initial displacement), with atomic-clock frequency-ratio drift limits and quasar absorption-line constraints on electron-mass variation; the primary paper flags this channel as future testability without extracting any quantitative bound.
  *Proposed resolution:* Compile published clock frequency-ratio drift limits (e.g. Yb/Cs, Al+/Hg+) and quasar mu = m_p/m_e variation measurements into a redshift-binned likelihood on Delta m_e/m_e(z), and evaluate it on the oscillating-plus-relaxing v(t) trajectory output by the axi-Higgs background solver.
- **Affleck-Dine L-ball resonant sterile-neutrino dark matter scenario** ([arXiv:2402.11902](https://arxiv.org/abs/2402.11902), score 39): X-ray line constraints on (m_s, sin^2 2theta) are only discussed qualitatively; no digitized exclusion contours or packaged flux likelihood accompany the card, so the viable-region claim cannot be re-evaluated quantitatively.
  *Proposed resolution:* Digitize the current combined X-ray limits (NuSTAR/Chandra/XMM blank-sky and M31 stacks) into a mass-mixing exclusion table and package it as a likelihood keyed to the model's decay rate, rescaled by the resonantly produced abundance.
- … and 137 more models (see `gap_roadmap.html`).

### Collider / beam-dump recast  —  `likelihood`
Priority weight 3,512 across 142 models (144 gap entries overall). Severity: blocks_key_observables × 54, degrades_precision × 75, minor × 15. Effort: medium × 112, small × 25, large × 7.

- **Inelastic dark matter with a dark photon mediator** ([arXiv:1911.03176](https://arxiv.org/abs/1911.03176), score 46): No public detector-level likelihood or recast module exists for the BaBar monophoton acceptance or the Belle II displaced photon-plus-dilepton/hadron vertex search applied to long-lived chi_2 decays; exclusions depend on unpublished escape-probability, veto, and trigger assumptions.
  *Proposed resolution:* Implement the BaBar monophoton and Belle II displaced-vertex analyses as recast cards in a long-lived-particle framework (e.g. a MadGraph event chain with parameterized detector geometry, photon/veto acceptances, and displaced-vertex trigger efficiencies), and publish the resulting efficiency maps as reusable likelihoods over (epsilon, m_A', m_chi1, Delta, alpha_D, c tau_chi2).
- **Type-I non-degenerate 2HDM+a dark matter benchmark model** ([arXiv:2404.05704](https://arxiv.org/abs/2404.05704), score 42): No validated LHC recast exists for the non-degenerate type-I 2HDM+a cascade and missing-energy benchmark topologies (h+MET, Z+MET, top-associated MET, visible heavy-Higgs cascades); existing degenerate type-II limits cannot be reused directly.
  *Proposed resolution:* Implement the relevant ATLAS/CMS Run 2 mono-Higgs, mono-Z, tt+MET and heavy-Higgs search analyses in MadAnalysis5 or CheckMATE, validate against at least one published mono-Higgs or mono-Z cutflow as the card's validation tests specify, and wire in the public simplified likelihoods where available.
- **Singlet-doublet freeze-in dark matter in fast-expansion cosmology** ([arXiv:2301.02514](https://arxiv.org/abs/2301.02514), score 41): No public collider recast or likelihood exists for the boosted W/Z/h plus missing-momentum jet-substructure MVA search; the FeynRules/MadGraph model files used by the authors are not public, so the quoted LHC mass reach cannot be reused or reinterpreted.
  *Proposed resolution:* Publish the FeynRules/UFO model files and implement the boosted-boson-plus-MET selection as a recast analysis in MadAnalysis5 or CheckMATE, including a simplified-likelihood surface over (m_2, c_tau, branching fractions).
- … and 141 more models (see `gap_roadmap.html`).

### CMB likelihood & ΔN_eff machinery  —  `likelihood`
Priority weight 3,005 across 119 models (120 gap entries overall). Severity: blocks_key_observables × 43, degrades_precision × 68, minor × 9. Effort: medium × 89, small × 24, large × 7.

- **Dodelson-Widrow sterile neutrino dark matter with active-neutrino self-interactions** ([arXiv:1910.04901](https://arxiv.org/abs/1910.04901), score 43): No implemented likelihood exists for the constraint overlay: X-ray line searches, BBN mediator bounds, and kaon-decay/DUNE laboratory limits are only compiled as figure regions from the paper, with likelihood_available set to no for both constraint entries.
  *Proposed resolution:* Assemble a composite likelihood: published X-ray line flux limits mapped to sin^2(2theta) vs m4 via the standard radiative decay rate, an N_eff/BBN term for the few-MeV scalar coupled to neutrinos (e.g. computed with a BBN code including the phi thermal population), and digitized K+ -> mu + nu phi limits, exposed over (m4, theta, m_phi, lambda_phi).
- **Flavor-blind Z-prime mediated complex scalar MeV dark matter** ([arXiv:2205.05714](https://arxiv.org/abs/2205.05714), score 40): No BBN light-element likelihood is implemented for this specific benchmark: the card's BBN/CMB constraint applies to broader light-WIMP classes and explicitly requires branch-by-branch recasting to the flavor-blind Z-prime scalar, which has not been done.
  *Proposed resolution:* Couple the solver output (time-dependent T_gamma/T_nu and expansion history) to a public BBN code such as PRIMAT, PArthENoPE, or AlterBBN to compute helium and deuterium yields and build a light-element likelihood for the benchmark's branching ratios.
- **Bose-Einstein-condensed scalar field dark matter** ([arXiv:1310.6061](https://arxiv.org/abs/1310.6061), score 39): There is no current-data likelihood for the SFDM expansion-history constraint: the quoted bounds on m and lambda/(mc^2)^2 derive from 2013-era BBN abundances and CMB equality values, and the card flags that recasting is needed before the allowed region can be trusted.
  *Proposed resolution:* Couple the gap_1 background solver to current Planck CMB and PRIMAT/PArthENoPE-style BBN likelihoods in Cobaya, sampling (m, lambda/(mc^2)^2) directly so the N_eff(t) and z_eq shifts are constrained by modern data rather than the historical 68% regions.
- … and 117 more models (see `gap_roadmap.html`).

### Lyman-α forest modeling & likelihood  —  `derivation`
Priority weight 2,964 across 106 models (106 gap entries overall). Severity: blocks_all_observables × 2, blocks_key_observables × 37, degrades_precision × 53, minor × 12, unknown × 2. Effort: medium × 96, small × 10.

- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): The ULP linear transfer function is not computed (transfer_function_shape unknown), so the scale of small-scale power suppression and its consistency with existing ULDM structure bounds cannot be checked for the three-pion system.
  *Proposed resolution:* Compute the linear perturbation evolution of the three oscillating pion fields with axionCAMB or the CLASS scalar-field module, extract the half-mode scale versus (m_pi, F_pi), and map onto published Lyman-alpha and subhalo ULDM mass bounds.
- **Dodelson-Widrow sterile neutrino dark matter with active-neutrino self-interactions** ([arXiv:1910.04901](https://arxiv.org/abs/1910.04901), score 43): The case-dependent nonthermal f_s(x,z) spectra (warmer or cooler than thermal) have not been propagated to a linear transfer function, so Lyman-alpha and small-scale-structure limits derived for thermal WDM or standard-DW spectra cannot be rigorously applied to this model's momentum distributions.
  *Proposed resolution:* Feed the solver's f_s spectra into CLASS's ncdm module with arbitrary distribution functions to compute T(k) across the parameter band, then recast Lyman-alpha flux-power constraints (via existing WDM/nu-s emulator mappings on the half-mode scale) instead of thermal-equivalent-mass rules of thumb.
- **Singlet-doublet freeze-in dark matter in fast-expansion cosmology** ([arXiv:2301.02514](https://arxiv.org/abs/2301.02514), score 41): The nonthermal momentum distribution f_chi1(p) from freeze-in decays and scatterings in the fast-expansion background has never been derived, so the warm-DM small-scale suppression cannot be computed or mapped onto thermal-WDM Lyman-alpha bounds.
  *Proposed resolution:* Solve the unintegrated (momentum-dependent) freeze-in Boltzmann equation for chi_1 including the modified H(T), feed the resulting tabulated f(p) into CLASS as a non-cold relic to get the transfer function, and map the cutoff onto an equivalent thermal-WDM mass for use with existing Lyman-alpha likelihoods/emulators.
- … and 103 more models (see `gap_roadmap.html`).

### Other / unclassified  —  `tool`
Priority weight 2,818 across 113 models (116 gap entries overall). Severity: blocks_all_observables × 12, blocks_key_observables × 58, degrades_precision × 38, minor × 8. Effort: medium × 79, large × 11, small × 26.

- **Type-I non-degenerate 2HDM+a dark matter benchmark model** ([arXiv:2404.05704](https://arxiv.org/abs/2404.05704), score 42): No public spectrum-and-precision-checks calculator covers the full 2HDM+a (2HDM plus singlet pseudoscalar with mixing); 2HDMC handles only the pure 2HDM sector, so theory/flavor/EW viability filtering of benchmark points is not automated.
  *Proposed resolution:* Extend 2HDMC (or write a light wrapper around the Pseudoscalar_2HDMI UFO parameters) to include the a-A mixing sector, and chain it to HiggsTools (HiggsBounds/HiggsSignals) plus standard perturbativity/unitarity/bounded-from-below checks for the non-degenerate mass planes.
- **Neutrino-coupled ultralight scalar dark matter** ([arXiv:2205.06821](https://arxiv.org/abs/2205.06821), score 39): No public calculator exists for active-sterile pseudo-Dirac oscillation probabilities with a time-dependent, DM-background-induced Majorana mass across the paper's constant, modulating, and random-walk coherence regimes, nor a likelihood-level comparison to solar and Super-Kamiokande atmospheric data; current bounds are order-of-magnitude m^2_lim translations.
  *Proposed resolution:* Implement the modulated pseudo-Dirac oscillation probability (averaging over scalar phase and coherence per baseline) as a module in a public neutrino-oscillation framework and recast published solar and Super-K disappearance likelihoods against it, replacing the m^2_lim < 1e-12 eV^2 and 1e-4 eV^2 proxy bounds.
- **GUT-scale supersymmetry with pseudo-Dirac Higgsino dark matter** ([arXiv:2001.01505](https://arxiv.org/abs/2001.01505), score 38): No public code implements the multi-scale EFT tower of this framework (two-Higgs-doublet plus Higgsinos below intermediate gaugino thresholds, matched to the MSSM/GUT at M_S) with two-loop running and threshold matching; the paper's SARAH-derived pipeline is not released, so per-mille unification residuals, the wino-scale ceiling, and tau_p cannot be reproduced.
  *Proposed resolution:* Build the split-spectrum EFT tower in FlexibleSUSY (or SARAH/SPheno) with successive matching at the gaugino and M_S thresholds, validate the per-mille unification residuals and the M2 < 2e5 GeV ceiling against the paper's scans, and output tau(p -> e+ pi0).
- … and 113 more models (see `gap_roadmap.html`).

### PBH formation & abundance pipeline  —  `likelihood`
Priority weight 2,634 across 102 models (106 gap entries overall). Severity: blocks_key_observables × 58, degrades_precision × 44, minor × 4. Effort: medium × 80, small × 22, large × 4.

- **N MACHOs** ([arXiv:1911.13281](https://arxiv.org/abs/1911.13281), score 47): Microlensing and femtolensing constraints on N-MACHOs are proxied by monochromatic point-lens PBH limits; no likelihood exists that folds in the OGLE and Subaru/HSC survey detection efficiencies, finite-source and finite-lens-size effects, and an extended N-MACHO mass function in the 10^-16 to 10^-8 solar-mass window.
  *Proposed resolution:* Build an event-rate likelihood from the published OGLE 5-year and Subaru/HSC M31 efficiency curves plus GRB femtolensing data, generalized to arbitrary lens mass functions and finite lens radii from the polytropic M-R relation, validated by recovering the published monochromatic PBH bounds.
- **Hidden-sector phase-transition axion minicluster dark matter** ([arXiv:1609.00208](https://arxiv.org/abs/1609.00208), score 36): No microlensing likelihood exists for extended AU-scale lenses in this scenario; point-MACHO limits are not directly applicable and survey efficiencies were never assembled.
  *Proposed resolution:* Build a likelihood from published EROS/OGLE/HSC-Subaru microlensing survey efficiency functions with finite-size and extended-lens magnification corrections for the predicted (M_m, R_m, f_MC) population.
- **Light scalar dark matter clumps with angular momentum** ([arXiv:1804.07255](https://arxiv.org/abs/1804.07255), score 36): No microlensing event-rate calculator or likelihood handling extended (finite-size) clump lenses is available; published Subaru HSC limits assume pointlike lenses, and the card's compactness constraint says extended clumps weaken direct application of those limits.
  *Proposed resolution:* Extend a public HSC/OGLE microlensing event-rate code with finite-lens-size magnification for the angular-momentum-modified R_90(M, l) relation and standard-halo kinematics, producing exclusion curves in (M, R_90, halo fraction).
- … and 103 more models (see `gap_roadmap.html`).

### Dwarf-galaxy dynamics & density profiles  —  `likelihood`
Priority weight 2,421 across 96 models (96 gap entries overall). Severity: blocks_key_observables × 42, degrades_precision × 51, minor × 3. Effort: medium × 64, small × 10, large × 22.

- **Dodelson-Widrow sterile-neutrino warm dark matter** ([arXiv:1011.2217](https://arxiv.org/abs/1011.2217), score 40): The dwarf-galaxy phase-space-density constraint on the DW sterile neutrino has no likelihood (likelihood_available: unknown), and the halo-history dependence of the coarse-grained Q_cg evolution is uncalibrated, leaving the Tremaine-Gunn-style bound qualitative.
  *Proposed resolution:* Construct a phase-space likelihood comparing the primordial Q of f_DW(y) with measured dwarf-spheroidal phase-space densities, using WDM simulations to calibrate how much Q_cg degrades during halo assembly.
- **Late Sommerfeld-enhanced dark-matter conversion to dark radiation** ([arXiv:1803.03644](https://arxiv.org/abs/1803.03644), score 40): No dwarf-galaxy self-interaction likelihood exists for the velocity-dependent sigma_T/m_chi(v) prediction; the route only supports an approximate threshold comparison at a single 30 km/s benchmark rather than a proper halo-population recast.
  *Proposed resolution:* Build a likelihood over published dwarf and low-surface-brightness halo fits (e.g. Jeans/isothermal SIDM halo-model inferences of sigma/m at measured velocity dispersions) that consumes sigma_T/m_chi(v) curves, replacing the single-velocity threshold cut.
- **Singlet-SUSY singlino self-interacting dark matter** ([arXiv:2204.01928](https://arxiv.org/abs/2204.01928), score 39): The SIDM small-scale-structure observable is evaluated only against the phenomenological 0.1-10 cm^2/g target window at v = 200 km/s; no simulation-calibrated halo prediction or astrophysical likelihood connects this model's velocity-dependent cross section to dwarf, LSB, or cluster data (simulation_calibrated is 'no', likelihood_available is 'no').
  *Proposed resolution:* Feed the model's velocity-dependent sigma(v)/m through an SIDM halo-modeling framework calibrated on SIDM N-body simulations (e.g. isothermal Jeans modeling of rotation curves and cluster profiles) to build a likelihood over the dwarf-to-cluster velocity range instead of a fixed target interval.
- … and 93 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `likelihood`
Priority weight 2,283 across 89 models (92 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 31, degrades_precision × 55, minor × 5. Effort: small × 35, medium × 51, large × 6.

- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): No numerical CMB isocurvature likelihood is implemented for the ULP misalignment initial conditions, so the H_inf/F_pi isocurvature bound exists only as the paper's analytic upper-limit formula.
  *Proposed resolution:* Implement the ULP as an axion-like uncorrelated CDM-isocurvature component in CLASS or CAMB and evaluate the Planck isocurvature likelihood over (m_pi, F_pi, H_inf), packaging it as a reusable module.
- **PQ Dirac-neutrino model with scalar WIMP and axion dark matter** ([arXiv:2001.01846](https://arxiv.org/abs/2001.01846), score 42): No packaged haloscope likelihood that applies the local axion-fraction rescaling Omega_a/Omega_CDM to ADMX-style exclusions exists for this multicomponent scenario; only raw exclusion curves assuming a full axion halo are available.
  *Proposed resolution:* Wrap the public ADMX limit tables in a likelihood function that rescales the signal power by the axion density fraction and the model's DFSZ g_agamma, e.g. as a module in a global-fit framework such as GAMBIT's DarkBit.
- **Millicharged co-interacting dark photon dark matter** ([arXiv:2403.07528](https://arxiv.org/abs/2403.07528), score 39): No reusable radio line-search likelihood exists for the Bose-enhanced dark-photon-to-photon conversion line: the SKA/LOFAR reach in the card is a one-hour sensitivity estimate without instrument response, foreground, or halo line-of-sight modeling packaged for reanalysis.
  *Proposed resolution:* Build a radio line-search module that combines an NFW/halo line-of-sight emissivity integral for the conversion rate with published LOFAR/SKA noise curves and a Galactic synchrotron foreground model, exposing it as a frequentist sensitivity/likelihood function over (m_A', epsilon, f_chi, g_d), patterned on existing axion/dark-photon radio-line search pipelines.
- … and 89 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `derivation`
Priority weight 2,203 across 81 models (82 gap entries overall). Severity: blocks_key_observables × 40, degrades_precision × 31, minor × 8, unknown × 3. Effort: medium × 54, small × 17, large × 11.

- **Nonthermal sub-keV light dark matter fraction from radiative decays** ([arXiv:1912.05563](https://arxiv.org/abs/1912.05563), score 52): The momentum distribution of the boosted decay-produced sub-keV component (born relativistic, redshifting through radiation domination) has not been derived in a form usable by a Boltzmann solver, so the mixed cold+hot transfer function for this scenario cannot be computed.
  *Proposed resolution:* Derive the analytic f(q) for two-body decay production during radiation domination as a function of tau and m_parent/m_DM, tabulate it into the CLASS ncdm module alongside a dominant CDM component, and compute the mixed transfer function and Delta N_eff self-consistently.
- **Scalar dark matter with a long-range dark fifth force** ([arXiv:2204.08484](https://arxiv.org/abs/2204.08484), score 46): No nonlinear perturbation-theory or simulation framework exists for dark-sector fifth forces with DM-baryon equivalence-principle violation, which the card identifies as required before galaxy power-spectrum and higher-point statistics from DESI/Euclid-like surveys can be used.
  *Proposed resolution:* Derive the beta-dependent perturbation-theory kernels including relative DM-baryon density and velocity modes, implement them in an EFT-of-LSS pipeline (CLASS-PT or PyBird style), and cross-check with a small suite of N-body simulations that include the dark-only fifth force.
- **Early mass-varying sterile-neutrino nugget dark matter with kination-diluted scalar EDE** ([arXiv:2005.11889](https://arxiv.org/abs/2005.11889), score 44): The microphysical nugget-formation dynamics (m_D, lambda, m_phi, m_psi1) and the phenomenological fluid transition used in the MCMC are related but not identical idealizations; no derived mapping exists from the Lagrangian parameters to the effective w(z), c_s^2(z), transition width, and the fraction of EDE energy entering kination versus nuggets.
  *Proposed resolution:* Derive the effective fluid parameters by evolving the coupled sterile-neutrino-scalar equations through the instability (linear perturbation theory plus a spherical-collapse or lattice treatment of nugget condensation), including the modified scalar potential required for kination rather than matter-like oscillation, and tabulate the resulting w(z) and energy partition for use in the Boltzmann solver.
- … and 79 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `simulation`
Priority weight 2,163 across 82 models (86 gap entries overall). Severity: blocks_all_observables × 5, blocks_key_observables × 41, degrades_precision × 38, minor × 2. Effort: large × 75, major_program × 7, medium × 4.

- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): No structure-formation pipeline exists for the mixed multi-field system of three dark pions plus superheavy dark baryons; halo and boson-star profiles for such pion/baryon admixtures are not calibrated by any simulation or solver referenced in the card.
  *Proposed resolution:* Extend a Schroedinger-Poisson solver such as PyUltraLight or AxioNyx to three coupled scalar fields with an additional cold particle component, and calibrate mixed halo/boson-star density profiles across the m_pi < 1 eV ULP regime.
- **Inflation-produced dark photon dark matter with Proca-star substructure** ([arXiv:2203.10100](https://arxiv.org/abs/2203.10100), score 43): No public Proca/vector Schroedinger-Poisson simulation pipeline exists that evolves inflation-seeded small-scale vector isocurvature perturbations through collapse into Proca-star solitons and compact halos; the paper's numerical simulations are not released, so soliton and compact-halo mass functions cannot be regenerated across (m, H_I).
  *Proposed resolution:* Extend an existing public wave-dark-matter Schroedinger-Poisson solver (e.g. PyUltraLight, AxioNyx, or GAMER's fuzzy-DM module) to three vector field components with initial conditions drawn from the inflationary longitudinal-mode power spectrum, and release calibrated soliton and compact-halo mass functions versus m and H_I.
- **B-L gauged Peccei-Quinn axion dark matter** ([arXiv:1805.10029](https://arxiv.org/abs/1805.10029), score 40): No defect-network simulation exists for the post-inflationary PQ/fiveness breaking histories of this model, in particular the 10:1 relatively-prime charge structure whose string-domain-wall decay can dominate the axion abundance; the paper explicitly leaves one domain-wall-resolution scenario to detailed numerical simulations, so the dominant-DM corner of parameter space cannot be quantitatively evaluated.
  *Proposed resolution:* Run lattice field-theory simulations of the coupled string-domain-wall network for the two-scalar (phi, phi_prime) fiveness potential with the 10:1 charge assignment, following the methodology of existing axion-string network simulations, and extract the network decay contribution to Omega_a h^2 and the domain-wall collapse condition as functions of F_A and the phase-transition ordering.
- … and 83 more models (see `gap_roadmap.html`).

### PBH formation & abundance pipeline  —  `tool`
Priority weight 2,068 across 84 models (89 gap entries overall). Severity: blocks_all_observables × 11, blocks_key_observables × 62, degrades_precision × 15, minor × 1. Effort: medium × 66, small × 17, large × 6.

- **Non-cold fermionic dark matter from primordial black hole evaporation** ([arXiv:2004.14773](https://arxiv.org/abs/2004.14773), score 36): No released pipeline connects the Hawking-emission spectrum and evaporation-history redshifting to the CLASS ncdm f(q) input; the paper's implementation is private, so the transfer-function route cannot be reproduced end to end.
  *Proposed resolution:* Write and release a small converter that takes BlackHawk (or analytic) emission spectra, applies the radiation- vs PBH-dominated redshifting branches, and outputs a CLASS-ready tabulated psd file, validated against the paper's transfer-function fits.
- **Heavy scalar dark matter from primordial-black-hole superradiance** ([arXiv:2205.11522](https://arxiv.org/abs/2205.11522), score 35): No public code solves the coupled system of Kerr PBH mass, spin, superradiant bound-state occupation, and DM number density used by the paper; the dedicated superradiance solver is not released, and existing PBH codes (BlackHawk, FRISBHEE, ULYSSES) omit the superradiance channel entirely.
  *Proposed resolution:* Extend a public PBH-cosmology framework (FRISBHEE or the ULYSSES PBH modules, with BlackHawk-style Kerr greybody evolution) by adding the analytic superradiance rate equations for the dominant scalar bound levels, and validate against the paper's factor-of-few to factor-20 enhancement contours in (M_i, a_star, mu).
- **PBH sterile neutrinogenesis of sterile neutrino dark matter** ([arXiv:2309.12258](https://arxiv.org/abs/2309.12258), score 35): The abundance calculation assumes a monochromatic PBH mass function and instantaneous evaporation; no implemented calculation handles extended mass functions and time-resolved evaporation for the sterile-neutrino yield and spectrum, which shifts both f_s and the warmness bound.
  *Proposed resolution:* Use or extend a public PBH evaporation code (BlackHawk-style time-dependent greybody emission) coupled to a background Friedmann solver to integrate sterile-neutrino production over extended mass functions and non-instantaneous reheating, outputting f_s and f(p) directly.
- … and 86 more models (see `gap_roadmap.html`).

## Top gaps blocking structure-formation observables

Restricted to gap entries that block an observable in a structure-formation channel (clusters, dwarf_galaxies, large_scale_structure, lyman_alpha, milky_way_satellites, small_scale_structure, strong_lensing), a route leading to one, or everything for a model with such observables; re-weighted accordingly.

### Lyman-α forest modeling & likelihood  —  `likelihood`
Priority weight 7,569 across 267 models (280 gap entries overall). Severity: blocks_key_observables × 132, degrades_precision × 147, minor × 1. Effort: medium × 223, large × 50, small × 7.

- **Nonthermal sub-keV light dark matter fraction from radiative decays** ([arXiv:1912.05563](https://arxiv.org/abs/1912.05563), score 52): The structure-formation bound f < 0.01 is inferred from generic mixed cold/hot arguments rather than from a dedicated likelihood: no Lyman-alpha, CMB, or sigma8 likelihood has been run against this model's boosted-subcomponent transfer function, so the actual upper limit on f versus m_DM and tau is uncalibrated.
  *Proposed resolution:* Recast an existing Lyman-alpha forest likelihood or emulator (e.g. the hydrodynamical-simulation-calibrated WDM/mixed-DM chains) plus Planck CMB data onto the mixed transfer functions from gap_2, producing f(m_DM, tau) exclusion contours.
- **Dark-fermion-production relaxion warm dark matter** ([arXiv:1909.07706](https://arxiv.org/abs/1909.07706), score 48): No public Lyman-alpha likelihood applies to this model's mixed case: the constraint is imposed only as the thermal-relic 5-5.3 keV m_WDM bound, which does not cover a partial warm fraction f_WDM < 1 or a diluted non-standard spectrum.
  *Proposed resolution:* Recast the model through an existing Lyman-alpha flux-power emulator/likelihood built for mixed cold-plus-warm dark matter, parameterized by (m_WDM_equivalent, f_WDM), using the transfer functions from the derived f(p) rather than the pure-thermal mapping.
- **Neutrino portal freeze-in dark matter** ([arXiv:1806.08579](https://arxiv.org/abs/1806.08579), score 45): No ready Lyman-alpha likelihood accepts the nonthermal freeze-in momentum distribution directly; the card notes the thermal-WDM template can misestimate the bound, and the exact transfer-function-to-likelihood mapping was not reproduced.
  *Proposed resolution:* Run the tabulated f(p) through CLASS ncdm to obtain T(k), then apply a shape-based Lyman-alpha recast (area criterion or the Murgia et al. alpha-beta-gamma parameterization with the MIKE/HIRES+XQ-100 likelihood) instead of the half-mode thermal-WDM mapping.
- … and 265 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `tool`
Priority weight 4,895 across 177 models (374 gap entries overall). Severity: blocks_all_observables × 33, blocks_key_observables × 281, degrades_precision × 54, minor × 6. Effort: small × 19, medium × 322, large × 33.

- **Decaying warm majoron dark matter** ([arXiv:1803.05650](https://arxiv.org/abs/1803.05650), score 52): No public Boltzmann-solver setup treats a keV-scale warm majoron with its thermal free-streaming distribution that also decays to relativistic neutrinos with tau_J of order 50 Gyr; the CLASS decaying-CDM module assumes cold parents, so the combined warm-plus-decaying linear evolution (T(k) and CMB spectra) cannot currently be computed.
  *Proposed resolution:* Extend CLASS by combining its non-cold-dark-matter (ncdm) sector with the decaying-dark-matter module: give the ncdm species a decay term sourcing massless neutrino dark radiation, parameterized by (m_J, tau_J, initial phase-space distribution), and validate against the paper's N-body initial conditions and the tau_J > 50 Gyr CMB benchmark.
- **Scalar dark matter with a long-range dark fifth force** ([arXiv:2204.08484](https://arxiv.org/abs/2204.08484), score 46): The modified CLASS implementing the dark fifth-force background (H(z), rho_dm(z), s(z)) and linear perturbations (relative DM-baryon density and velocity modes) is not publicly released, so the whole CMB+BAO pipeline cannot be rerun or extended by others.
  *Proposed resolution:* Obtain or reimplement the fifth-force patch in public CLASS: add the scalar-field background with m_chi(s) coupling and beta, m_s/H_0 parameters plus the coupled DM-baryon perturbation equations, and validate against the published beta < 0.011 (Planck) and beta < 0.0054 (Planck+BAO) bounds.
- **Non-canonical asymptotically-flat coth-potential unified dark sector** ([arXiv:1803.09767](https://arxiv.org/abs/1803.09767), score 46): No Boltzmann-code implementation exists for the non-canonical UDM scalar with L=X(X/M^4)^(alpha-1)-V(phi) and the asymptotically-flat coth potential: neither CLASS nor CAMB evolves the perturbations of a single field whose kinetic energy clusters as dark matter with c_s^2=(2 alpha-1)^-1 while its potential drives acceleration, so the transfer function and Jeans-scale viability test cannot be computed.
  *Proposed resolution:* Extend CLASS with a k-essence/UDM module that takes the background solution for this potential and evolves the exact scalar perturbation equations (or, as a first pass, map the model onto CLASS's generalized-dark-matter fluid with w(a) from the background solve and constant c_s^2=(2 alpha-1)^-1), validating the pressureless large-alpha limit against the paper.
- … and 179 more models (see `gap_roadmap.html`).

### Dwarf-galaxy dynamics & density profiles  —  `likelihood`
Priority weight 1,937 across 73 models (96 gap entries overall). Severity: blocks_key_observables × 42, degrades_precision × 51, minor × 3. Effort: medium × 64, small × 10, large × 22.

- **Dodelson-Widrow sterile-neutrino warm dark matter** ([arXiv:1011.2217](https://arxiv.org/abs/1011.2217), score 40): The dwarf-galaxy phase-space-density constraint on the DW sterile neutrino has no likelihood (likelihood_available: unknown), and the halo-history dependence of the coarse-grained Q_cg evolution is uncalibrated, leaving the Tremaine-Gunn-style bound qualitative.
  *Proposed resolution:* Construct a phase-space likelihood comparing the primordial Q of f_DW(y) with measured dwarf-spheroidal phase-space densities, using WDM simulations to calibrate how much Q_cg degrades during halo assembly.
- **Late Sommerfeld-enhanced dark-matter conversion to dark radiation** ([arXiv:1803.03644](https://arxiv.org/abs/1803.03644), score 40): No dwarf-galaxy self-interaction likelihood exists for the velocity-dependent sigma_T/m_chi(v) prediction; the route only supports an approximate threshold comparison at a single 30 km/s benchmark rather than a proper halo-population recast.
  *Proposed resolution:* Build a likelihood over published dwarf and low-surface-brightness halo fits (e.g. Jeans/isothermal SIDM halo-model inferences of sigma/m at measured velocity dispersions) that consumes sigma_T/m_chi(v) curves, replacing the single-velocity threshold cut.
- **Singlet-SUSY singlino self-interacting dark matter** ([arXiv:2204.01928](https://arxiv.org/abs/2204.01928), score 39): The SIDM small-scale-structure observable is evaluated only against the phenomenological 0.1-10 cm^2/g target window at v = 200 km/s; no simulation-calibrated halo prediction or astrophysical likelihood connects this model's velocity-dependent cross section to dwarf, LSB, or cluster data (simulation_calibrated is 'no', likelihood_available is 'no').
  *Proposed resolution:* Feed the model's velocity-dependent sigma(v)/m through an SIDM halo-modeling framework calibrated on SIDM N-body simulations (e.g. isothermal Jeans modeling of rotation curves and cluster profiles) to build a likelihood over the dwarf-to-cluster velocity range instead of a fixed target interval.
- … and 70 more models (see `gap_roadmap.html`).

### Relic-abundance solver extension (micrOMEGAs/MadDM/DarkSUSY)  —  `tool`
Priority weight 1,682 across 61 models (480 gap entries overall). Severity: blocks_all_observables × 33, blocks_key_observables × 287, degrades_precision × 138, minor × 22. Effort: medium × 304, large × 25, small × 151.

- **Inelastic freeze-in pseudo-Dirac dark matter** ([arXiv:2304.06072](https://arxiv.org/abs/2304.06072), score 39): No public model implementation of the pseudo-Dirac dark-photon freeze-in setup exists; the paper's micrOMEGAs/CalcHEP model files for computing the chi1/chi2 freeze-in yields (including Z-decay contributions) are not released.
  *Proposed resolution:* Reimplement the pseudo-Dirac model (chi1, chi2, kinetically mixed A' with off-diagonal current) in CalcHEP/FeynRules and run micrOMEGAs freeze-in routines, validating against the paper's relic-density contours.
- **Messenger-inflation minimal GMSB gravitino superWIMP** ([arXiv:2103.11256](https://arxiv.org/abs/2103.11256), score 38): The paper's spectrum and relic pipeline is unreleased: the mapping from (Lambda, M, N_5, tan beta) to the sparticle spectrum satisfying the 125.1 GeV Higgs constraint and the bino freeze-out abundance cannot be reproduced from the card alone, which blocks any independent rescan of the messenger-inflation-consistent parameter space.
  *Proposed resolution:* Reimplement the minimal GMSB pipeline with public tools (a standard MSSM spectrum generator with GMSB boundary conditions plus a relic-density code for slepton-coannihilating bino freeze-out), validating against the paper's benchmark (tau_B < 1 s at N_5=1, Lambda=10, m_3/2 < 5.8 GeV).
- **Radiative seesaw warm N_1 dark matter with N_2 entropy dilution** ([arXiv:1208.2613](https://arxiv.org/abs/1208.2613), score 37): No reusable scan pipeline couples the thermal yield, N_2 entropy injection, BBN reheating bound (0.7-4 MeV), relic density, and LFV constraints; the paper provides only analytic estimates for benchmark points (agent_notes: no reusable global scan).
  *Proposed resolution:* Implement the coupled yield/dilution ODE system with a BBN reheating-temperature check as a small standalone solver (or a micrOMEGAs non-standard-cosmology module) and combine it with the analytic LFV formulas into a parameter-scan driver.
- … and 60 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `likelihood`
Priority weight 1,558 across 56 models (140 gap entries overall). Severity: blocks_key_observables × 49, degrades_precision × 86, minor × 5. Effort: medium × 96, small × 37, large × 7.

- **Decaying warm majoron dark matter** ([arXiv:1803.05650](https://arxiv.org/abs/1803.05650), score 52): The Ly-alpha forest constraint is available only as a 5.3 keV thermal-relic-WDM lower bound (XQ-100 plus HIRES/MIKE); no public flux-power likelihood or emulator accepts the decaying-warm-majoron transfer function directly, so applying the bound requires an ad hoc thermal-equivalent mapping that the card flags as approximate.
  *Proposed resolution:* Recast via a general small-scale-suppression Ly-alpha emulator: evaluate the majoron T(k) from the extended Boltzmann code and pass it through an existing Ly-alpha flux-power emulator framework trained on generalized cutoff shapes (alpha-beta-gamma parameterization), calibrating any decay-induced late-time suppression with a small set of dedicated hydrodynamic or N-body runs.
- **Non-canonical asymptotically-flat coth-potential unified dark sector** ([arXiv:1803.09767](https://arxiv.org/abs/1803.09767), score 46): No data-comparison pipeline exists for this potential: the paper is a background demonstration with no likelihood table, and no implemented analysis confronts H(z), distances, and P(k) of the coth-potential UDM with CMB, BAO, SN, and LSS data.
  *Proposed resolution:* Once the perturbation module exists, wrap the model as a Cobaya theory class and run it against Planck 2018, BAO, and Pantheon+ likelihoods plus a linear P(k)/S8 probe to obtain posteriors on (alpha, M, V0, initial conditions).
- **Non-canonical cascading step-potential unified dark sector** ([arXiv:1803.09767](https://arxiv.org/abs/1803.09767), score 46): No data-comparison pipeline exists for this potential: the paper is a background demonstration with no likelihood table, and no implemented analysis confronts H(z), distances, and P(k) of the cascading-step-potential UDM with CMB, BAO, SN, and LSS data.
  *Proposed resolution:* Once the perturbation module exists, wrap the model as a Cobaya theory class and run it against Planck 2018, BAO, and Pantheon+ likelihoods plus a linear P(k)/S8 probe to obtain posteriors on (alpha, M, V0, step parameters, initial conditions).
- … and 53 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `tool`
Priority weight 1,512 across 53 models (134 gap entries overall). Severity: blocks_all_observables × 10, blocks_key_observables × 87, degrades_precision × 34, minor × 3. Effort: medium × 94, large × 28, small × 12.

- **Fuzzy dark matter with repulsive quartic self-interaction** ([arXiv:1805.08112](https://arxiv.org/abs/1805.08112), score 46): The modified CLASS module implementing the quartic-FDM effective-fluid sound speed c_s^2(a,k) and the quartic-dominated (radiation-like) background phase is not publicly released; without it neither the matter-power cutoff nor the equality-shift observable can be recomputed or the lambda(m) bound updated with current data.
  *Proposed resolution:* Reimplement the paper's averaged scalar-fluid treatment as a public CLASS extension (natural host: extend AxiCLASS's ultralight-axion fluid module with the lambda phi^4 correction to w(a) and c_s^2(a,k)), validate against the paper's quoted log10(lambda) = -91.86 + 4 log10(m/1e-22 eV) exclusion line, and couple it to MontePython/Cobaya with current Planck and LSS likelihoods.
- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): No detector-level signal model exists for the millicharged-pion and pi0-photon couplings; the paper defers resonant-detector and modified-electrodynamics forecasts to future work, so laboratory observability cannot be evaluated.
  *Proposed resolution:* Derive the g_pi_gamma and millicharge signal power in axion-haloscope and light-shining-through-wall setups, and recast existing haloscope/millicharge exclusion data onto the (epsilon, m_pi, F_pi) plane.
- **Extreme axion dark matter with cosine potential fluid modeling** ([arXiv:2311.02052](https://arxiv.org/abs/2311.02052), score 42): The modified axionCAMB implementing the full cosine potential, extreme hilltop initial angles, the effective-sound-speed fluid switch, and the background lookup-table acceleration is described in the paper but not publicly released, so no one else can compute CMB or matter power spectra for extreme axions.
  *Proposed resolution:* Obtain or reimplement the cosine-potential fluid treatment as a public axionCAMB (or CLASS) patch, including the shooting solver for f_a given (m_a, theta_i, Omega_a h^2) and validation against the paper's field-equation comparisons.
- … and 50 more models (see `gap_roadmap.html`).

### Lyman-α forest modeling & likelihood  —  `derivation`
Priority weight 1,494 across 52 models (106 gap entries overall). Severity: blocks_all_observables × 2, blocks_key_observables × 37, degrades_precision × 53, minor × 12, unknown × 2. Effort: medium × 96, small × 10.

- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): The ULP linear transfer function is not computed (transfer_function_shape unknown), so the scale of small-scale power suppression and its consistency with existing ULDM structure bounds cannot be checked for the three-pion system.
  *Proposed resolution:* Compute the linear perturbation evolution of the three oscillating pion fields with axionCAMB or the CLASS scalar-field module, extract the half-mode scale versus (m_pi, F_pi), and map onto published Lyman-alpha and subhalo ULDM mass bounds.
- **Singlet-doublet freeze-in dark matter in fast-expansion cosmology** ([arXiv:2301.02514](https://arxiv.org/abs/2301.02514), score 41): The nonthermal momentum distribution f_chi1(p) from freeze-in decays and scatterings in the fast-expansion background has never been derived, so the warm-DM small-scale suppression cannot be computed or mapped onto thermal-WDM Lyman-alpha bounds.
  *Proposed resolution:* Solve the unintegrated (momentum-dependent) freeze-in Boltzmann equation for chi_1 including the modified H(T), feed the resulting tabulated f(p) into CLASS as a non-cold relic to get the transfer function, and map the cutoff onto an equivalent thermal-WDM mass for use with existing Lyman-alpha likelihoods/emulators.
- **Twin sterile neutrino dark matter** ([arXiv:2305.06364](https://arxiv.org/abs/2305.06364), score 41): The cooled N_B momentum distribution after twin-sector production and SM-bath entropy injection is not tabulated, so structure constraints rest on an approximate thermal-WDM mass rescaling (M_min ~ 25 keV x T_B/T_A) instead of actual transfer functions and warmness likelihoods.
  *Proposed resolution:* Output the momentum-resolved f(p) from the twin-sector production solver (gap_1 tool), feed it to CLASS as a tabulated ncdm distribution to get the matter-power cutoff, and map onto existing Lyman-alpha/Milky-Way-satellite warm-DM likelihoods via the half-mode mass.
- … and 49 more models (see `gap_roadmap.html`).

### Boltzmann-code extension (CLASS/CAMB/ETHOS)  —  `derivation`
Priority weight 1,415 across 51 models (82 gap entries overall). Severity: blocks_key_observables × 40, degrades_precision × 31, minor × 8, unknown × 3. Effort: medium × 54, small × 17, large × 11.

- **Nonthermal sub-keV light dark matter fraction from radiative decays** ([arXiv:1912.05563](https://arxiv.org/abs/1912.05563), score 52): The momentum distribution of the boosted decay-produced sub-keV component (born relativistic, redshifting through radiation domination) has not been derived in a form usable by a Boltzmann solver, so the mixed cold+hot transfer function for this scenario cannot be computed.
  *Proposed resolution:* Derive the analytic f(q) for two-body decay production during radiation domination as a function of tau and m_parent/m_DM, tabulate it into the CLASS ncdm module alongside a dominant CDM component, and compute the mixed transfer function and Delta N_eff self-consistently.
- **Scalar dark matter with a long-range dark fifth force** ([arXiv:2204.08484](https://arxiv.org/abs/2204.08484), score 46): No nonlinear perturbation-theory or simulation framework exists for dark-sector fifth forces with DM-baryon equivalence-principle violation, which the card identifies as required before galaxy power-spectrum and higher-point statistics from DESI/Euclid-like surveys can be used.
  *Proposed resolution:* Derive the beta-dependent perturbation-theory kernels including relative DM-baryon density and velocity modes, implement them in an EFT-of-LSS pipeline (CLASS-PT or PyBird style), and cross-check with a small suite of N-body simulations that include the dark-only fifth force.
- **Conformal freeze-in quark-portal dark matter** ([arXiv:1910.10160](https://arxiv.org/abs/1910.10160), score 40): No derivation exists of the confined keV-scale dark-matter momentum distribution from the CFT energy density after QCD-induced conformal breaking; the paper tracks energy density only, so the model-specific f(q) needed for a precision warm-DM recast is absent.
  *Proposed resolution:* Derive or parameterize the confinement mapping — e.g. assume dark-sector self-thermalization at the gap scale with a parameterized mean momentum and width, bracketed by extreme energy-partition scenarios — and tabulate the resulting f(q) as input to a Boltzmann solver, checking sensitivity of the transfer function to the ansatz.
- … and 48 more models (see `gap_roadmap.html`).

### Milky Way satellite / subhalo-count likelihood  —  `simulation`
Priority weight 1,384 across 48 models (50 gap entries overall). Severity: blocks_key_observables × 17, degrades_precision × 32, minor × 1. Effort: large × 48, major_program × 2.

- **Heavy-scalar-decay non-thermal warm dark matter** ([arXiv:2305.15736](https://arxiv.org/abs/2305.15736), score 50): Nonlinear predictions (subhalo mass function, satellite counts) rely on thermal-WDM-calibrated simulations through an approximate half-mode-scale matching; no N-body simulations exist for the broader, higher-momentum-peaked decay-produced spectra, so the mapping's validity outside the studied f(q) grid is uncontrolled.
  *Proposed resolution:* Run a small suite of cosmological N-body simulations initialized with the modified-CLASS nonthermal transfer functions (and thermal velocities drawn from f(q)) for a few benchmark m_nonthermal values, and calibrate the subhalo mass function against the thermal-WDM equivalents to validate or correct the half-mode mapping.
- **Two-state inelastic self-interacting dark matter** ([arXiv:1805.03203](https://arxiv.org/abs/1805.03203), score 46): All existing simulations for this benchmark are dark-matter-only; the card notes baryonic feedback is a major missing ingredient for mapping the predicted cores and satellite depletion onto observed Milky Way dwarfs.
  *Proposed resolution:* Run zoom-in hydrodynamical simulations of Milky Way-like halos with the inelastic two-state scattering module coupled to an established galaxy-formation model (e.g. Auriga/IllustrisTNG physics in Arepo), spanning the benchmark (sigma/m, Delta/m_chi) grid.
- **Early mass-varying sterile-neutrino nugget dark matter, simple CDM-transition variant** ([arXiv:2005.11889](https://arxiv.org/abs/2005.11889), score 41): Nonlinear collapse of the unstable sterile-neutrino fluid into nuggets is not simulated anywhere; the sharp small-scale cutoff in the matter power spectrum and the halo/subhalo mass function effects are only discussed qualitatively, so the shape and scale of the cutoff transfer function are uncalibrated.
  *Proposed resolution:* Run dedicated simulations of the scalar-mediated instability (coupled fermion-fluid plus scalar-field dynamics, e.g. an adapted cosmological hydrodynamics or N-body plus scalar solver) to obtain the post-formation nugget mass spectrum and the calibrated cutoff transfer function as a function of m_phi and z_F.
- … and 45 more models (see `gap_roadmap.html`).

### Indirect-detection likelihood (γ-ray / cosmic-ray / ν)  —  `likelihood`
Priority weight 1,204 across 45 models (343 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 159, degrades_precision × 162, minor × 21. Effort: medium × 258, small × 56, large × 29.

- **Thermal dark matter freeze-out during kination** ([arXiv:2304.12336](https://arxiv.org/abs/2304.12336), score 40): No packaged likelihood pipeline connects the kination-enhanced microhalo boost factor to Fermi-LAT dwarf-spheroidal, H.E.S.S. Galactic Center, and isotropic gamma-ray background limits; the constraint currently requires a manual channel-by-channel recast through the boost factor.
  *Proposed resolution:* Wire the microhalo-models boost output into the released Fermi-LAT dwarf-spheroidal bin-by-bin likelihoods and published H.E.S.S. limit curves, with annihilation spectra per channel from PPPC4DMID or HDMSpectra, exposing (m_DM, <sigma v>, T_KR, T_kd, channel) as inputs.
- **Scalar singlet FIMP dark matter with vectorlike fermion parent during reheating** ([arXiv:2306.17238](https://arxiv.org/abs/2306.17238), score 37): LLP exclusions for the SM-charged vectorlike fermion parent decaying to s plus a right-handed lepton are not packaged: published heavy-stable-charged-particle and displaced-lepton searches must be recast with model-specific production cross sections and detector acceptances not encoded in the card.
  *Proposed resolution:* Implement the vectorlike lepton parent in an existing LLP recast framework (e.g. the LLP Recasting Repository or SModelS displaced/HSCP results) for the muonphilic benchmark, producing exclusion contours in the (m_P, c tau_P) plane that can be overlaid on the relic-density-required decay lengths per reheating history.
- **Early matter domination with nearly degenerate daughter WIMP dark matter** ([arXiv:1311.4034](https://arxiv.org/abs/1311.4034), score 36): No public likelihood exists for the Fermi-LAT isotropic gamma-ray background and dwarf-galaxy constraints on the microhalo-boosted annihilation signal of this model, nor for the BBN reheating/branching consistency requirement; existing EMD gamma-ray bounds assume different free-streaming and final-state choices and must be recast.
  *Proposed resolution:* Recast the Fermi-LAT IGRB and Draco analyses of arXiv:1910.08553 into a reusable likelihood parameterized by (<sigma v>, annihilation final state, microhalo boost factor), and add an analytic BBN consistency check on T_RH > T_BBN with the f_b relation as a prior.
- … and 42 more models (see `gap_roadmap.html`).

### Sterile-neutrino production solver  —  `tool`
Priority weight 1,198 across 44 models (61 gap entries overall). Severity: blocks_all_observables × 15, blocks_key_observables × 39, degrades_precision × 7. Effort: medium × 40, large × 19, small × 2.

- **Resonantly produced sterile neutrino dark matter in non-standard pre-BBN cosmology** ([arXiv:1911.03398](https://arxiv.org/abs/1911.03398), score 41): No public resonant sterile-neutrino production solver supports a non-standard pre-BBN expansion history: existing quantum-kinetic/Boltzmann codes (e.g. sterile-dm for Shi-Fuller production) hard-code the standard H(T), so the model's central parameterization of a modified Hubble rate above T_tr cannot be scanned with existing tools.
  *Proposed resolution:* Extend a public Shi-Fuller solver (sterile-dm or an averaged-Boltzmann reimplementation) to take a parameterized H(T) with power-law index and transition temperature T_tr as inputs, tracking lepton-asymmetry depletion and entropy conservation, and validate against the paper's example cosmology wedges in the mass-mixing plane.
- **Non-resonantly produced sterile neutrino dark matter in non-standard pre-BBN cosmology** ([arXiv:1909.13328](https://arxiv.org/abs/1909.13328), score 40): No public sterile-neutrino production solver computes non-resonant freeze-in in non-standard pre-BBN expansion histories; existing Dodelson-Widrow codes (sterile-dm class) hard-code standard radiation domination and cannot scan H(T), T_tr, or entropy-producing low-reheating histories.
  *Proposed resolution:* Extend a public Dodelson-Widrow solver (or implement the paper's closed-form production kernels) with a parameterized pre-BBN module taking H(T), T_tr, and entropy injection as inputs, outputting f_s(p) and Omega_s h^2 over the (m_s, sin^2 2theta) plane for kination, scalar-tensor, and low-reheating cosmologies.
- **Affleck-Dine L-ball resonant sterile-neutrino dark matter scenario** ([arXiv:2402.11902](https://arxiv.org/abs/2402.11902), score 39): No public sterile-neutrino kinetic solver couples Shi-Fuller resonant production to a time-dependent lepton asymmetry L_nue(T) released by L-ball decay; existing public resonant-production codes assume a primordial (pre-existing) asymmetry, and the paper's numerical framework was not released, with the stationary-point approximation additionally limiting validity below m_s ~ 10-20 keV.
  *Proposed resolution:* Extend a public resonant sterile-neutrino production code (e.g. the sterile-dm solver of Venumadhav et al.) to accept an externally supplied L_nue(T) injection history from L-ball decay, solving the full momentum-dependent quantum kinetic equation to remove the stationary-point restriction, and validate against the paper's abundance contours and free-streaming lengths.
- … and 41 more models (see `gap_roadmap.html`).

### Other / unclassified  —  `likelihood`
Priority weight 1,181 across 45 models (140 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 54, degrades_precision × 70, minor × 15. Effort: small × 42, medium × 93, large × 5.

- **Affleck-Dine L-ball resonant sterile-neutrino dark matter scenario** ([arXiv:2402.11902](https://arxiv.org/abs/2402.11902), score 39): X-ray line constraints on (m_s, sin^2 2theta) are only discussed qualitatively; no digitized exclusion contours or packaged flux likelihood accompany the card, so the viable-region claim cannot be re-evaluated quantitatively.
  *Proposed resolution:* Digitize the current combined X-ray limits (NuSTAR/Chandra/XMM blank-sky and M31 stacks) into a mass-mixing exclusion table and package it as a likelihood keyed to the model's decay rate, rescaled by the resonantly produced abundance.
- **Abelian Z' sterile-neutrino progenitor dark matter variants** ([arXiv:1602.06651](https://arxiv.org/abs/1602.06651), score 36): No reusable X-ray line likelihood (with marginalization over halo column density and decay branching) is available for the radiative decay of the keV sterile state; only published exclusion curves under simple assumptions exist.
  *Proposed resolution:* Package XMM-Newton/Chandra M31, Milky Way, and cluster line searches into a public likelihood over (M_s, sin^2 2theta, f_DM) with a documented column-density prior, so subcomponent and small-mixing scenarios can be scored consistently.
- **PBH sterile neutrinogenesis of sterile neutrino dark matter** ([arXiv:2309.12258](https://arxiv.org/abs/2309.12258), score 35): No public X-ray line likelihood covers the sterile-neutrino decay signal as a function of (m_s, sin^2 2theta, f_s) with astrophysical background modeling, so the 3.5 keV target region and XRISM-era sensitivity cannot be evaluated statistically for this model.
  *Proposed resolution:* Package existing blank-sky, M31, and cluster X-ray line search data into a reusable likelihood over line energy and flux with target J/D-factors, parameterized so the decay rate scales with sin^2 2theta and the abundance with f_s.
- … and 42 more models (see `gap_roadmap.html`).

### Indirect-detection likelihood (γ-ray / cosmic-ray / ν)  —  `tool`
Priority weight 1,160 across 45 models (247 gap entries overall). Severity: blocks_all_observables × 15, blocks_key_observables × 164, degrades_precision × 55, minor × 13. Effort: medium × 157, small × 72, large × 18.

- **Decaying warm dark matter with a massive dark-photon daughter** ([arXiv:2006.12348](https://arxiv.org/abs/2006.12348), score 38): No public Boltzmann-solver branch evolves a nonthermal (scalar-decay-produced) fermionic WDM parent that subsequently decays into a massive keV dark photon plus massless dark radiation, so the matter-power cutoff and CMB energy-transfer consistency cannot be computed from the actual phase-space distribution.
  *Proposed resolution:* Extend a CLASS decaying-CDM branch with massive daughters (e.g. the DCDM-to-warm-daughter implementations) to take the scalar-decay-produced parent f(q) as a tabulated ncdm input and add the two-body decay to a massive vector plus dark-radiation species, outputting T(k) and the free-streaming scale for the 0.3-0.5 Mpc target.
- **U(1)' dark-photon self-interacting dark matter from a MeV first-order phase transition** ([arXiv:2306.16966](https://arxiv.org/abs/2306.16966), score 38): The CMB annihilation check for symmetric or partially asymmetric populations is not implemented: the Sommerfeld-enhanced, dark-photon-mediated annihilation with subsequent A'/phi decay to e+e- through the dimension-six operator is not wired into any energy-injection code.
  *Proposed resolution:* Implement the model's cascade annihilation spectrum and Sommerfeld factor S(v) in DarkHistory or ExoCLASS and evaluate the Planck p_ann constraint as a function of the residual symmetric fraction set by Y_chi.
- **Scalar singlet FIMP dark matter with vectorlike fermion parent during reheating** ([arXiv:2306.17238](https://arxiv.org/abs/2306.17238), score 37): No public Boltzmann solver computes scalar-singlet freeze-in from decays of a thermalized vectorlike fermion parent during non-instantaneous monomial reheating (m_P > T_rh), including entropy injection and bosonic versus fermionic reheating histories; the yield exists only in the paper's private integration.
  *Proposed resolution:* Extend micrOMEGAs' freeze-in routines (or release the paper's solver) to evolve the DM yield through a user-specified reheating history parameterized by (T_rh, k, bosonic/fermionic inflaton decay), validated against the radiation-dominated limit for m_P < T_rh and the stated factor-of-two rate difference from the charged-scalar-parent variant.
- … and 42 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `simulation`
Priority weight 1,086 across 39 models (86 gap entries overall). Severity: blocks_all_observables × 5, blocks_key_observables × 41, degrades_precision × 38, minor × 2. Effort: large × 75, major_program × 7, medium × 4.

- **Ultra-Light Pion and Baryon WIMPzilla dark matter** ([arXiv:2205.12983](https://arxiv.org/abs/2205.12983), score 45): No structure-formation pipeline exists for the mixed multi-field system of three dark pions plus superheavy dark baryons; halo and boson-star profiles for such pion/baryon admixtures are not calibrated by any simulation or solver referenced in the card.
  *Proposed resolution:* Extend a Schroedinger-Poisson solver such as PyUltraLight or AxioNyx to three coupled scalar fields with an additional cold particle component, and calibrate mixed halo/boson-star density profiles across the m_pi < 1 eV ULP regime.
- **Inflation-produced dark photon dark matter with Proca-star substructure** ([arXiv:2203.10100](https://arxiv.org/abs/2203.10100), score 43): No public Proca/vector Schroedinger-Poisson simulation pipeline exists that evolves inflation-seeded small-scale vector isocurvature perturbations through collapse into Proca-star solitons and compact halos; the paper's numerical simulations are not released, so soliton and compact-halo mass functions cannot be regenerated across (m, H_I).
  *Proposed resolution:* Extend an existing public wave-dark-matter Schroedinger-Poisson solver (e.g. PyUltraLight, AxioNyx, or GAMER's fuzzy-DM module) to three vector field components with initial conditions drawn from the inflationary longitudinal-mode power spectrum, and release calibrated soliton and compact-halo mass functions versus m and H_I.
- **Late-PQ-phase-transition axion parametric-resonance dark matter** ([arXiv:1910.04163](https://arxiv.org/abs/1910.04163), score 38): No public lattice/field-evolution simulation exists for saxion parametric resonance after a late PQ phase transition, including the thermal PQ potential, backreaction, and dissipation into SM fields, so the axion yield and momentum spectrum rest on analytic scaling estimates only.
  *Proposed resolution:* Implement the radial PQ-field potential with its thermal correction and the y P psi psi dissipation channel in a public lattice code such as CosmoLattice or LATTICEEASY, scan (f_a, m_s, T_c, n), and output the axion number density and momentum spectrum at the end of resonance.
- … and 40 more models (see `gap_roadmap.html`).

### Self-interacting DM halo modeling  —  `tool`
Priority weight 832 across 33 models (44 gap entries overall). Severity: blocks_all_observables × 4, blocks_key_observables × 27, degrades_precision × 11, minor × 2. Effort: medium × 29, small × 8, large × 7.

- **Two-state inelastic self-interacting dark matter** ([arXiv:1805.03203](https://arxiv.org/abs/1805.03203), score 46): The Arepo module implementing two-state inelastic (endothermic/exothermic) self-scattering is author code with no extracted public repository, so the simulation step of the only route cannot be reproduced or extended to new (sigma/m, Delta/m_chi, f_excited) points by others.
  *Proposed resolution:* Obtain public release of the authors' Arepo inelastic-SIDM patch, or reimplement the two-state velocity-dependent scattering kernel (elastic, up-scattering, and exothermic down-scattering with velocity kicks) in a public N-body code with an existing SIDM module such as Gadget-4 or the public Arepo, validated against the paper's five-times-elastic-equivalence benchmark.
- **Bose-Einstein-condensed scalar field dark matter** ([arXiv:1310.6061](https://arxiv.org/abs/1310.6061), score 39): No public background solver implements the complex SFDM with conserved U(1) charge and quartic self-interaction: the stiff, radiationlike, and CDM-like phase transitions that set N_eff and z_eq must be reproduced from the primary-paper equations, since standard CLASS/CAMB scalar-field modules do not cover this charge-conserving, self-interacting condensate.
  *Proposed resolution:* Write a standalone FRW + Klein-Gordon background integrator (or a CLASS background extension) taking (m, lambda/(mc^2)^2, Q) as inputs, using the fast-oscillation effective-fluid approximation in the oscillatory regime and validating against the quoted phase-transition redshifts and the m >= 2.4e-21 eV/c^2 bound.
- **Self-interacting dark matter from late pseudo-WIMP decays** ([arXiv:2006.16139](https://arxiv.org/abs/2006.16139), score 37): Public CLASS DCDM implementations cover decay to dark radiation or to a single massive daughter, but not this model's combined final state of massive self-interacting daughters with a tunable subdominant dark-radiation fraction F and daughter kick velocity.
  *Proposed resolution:* Extend the massive-daughter DCDM branch of CLASS (Abellan et al. type) with a branching fraction F into dark radiation and the daughter phase-space distribution from the mass splitting delta, then rerun the MontePython CMB/BAO/SN scans to reproduce the paper's two preferred regions.
- … and 30 more models (see `gap_roadmap.html`).

### PBH formation & abundance pipeline  —  `likelihood`
Priority weight 828 across 29 models (106 gap entries overall). Severity: blocks_key_observables × 58, degrades_precision × 44, minor × 4. Effort: medium × 80, small × 22, large × 4.

- **N MACHOs** ([arXiv:1911.13281](https://arxiv.org/abs/1911.13281), score 47): Microlensing and femtolensing constraints on N-MACHOs are proxied by monochromatic point-lens PBH limits; no likelihood exists that folds in the OGLE and Subaru/HSC survey detection efficiencies, finite-source and finite-lens-size effects, and an extended N-MACHO mass function in the 10^-16 to 10^-8 solar-mass window.
  *Proposed resolution:* Build an event-rate likelihood from the published OGLE 5-year and Subaru/HSC M31 efficiency curves plus GRB femtolensing data, generalized to arbitrary lens mass functions and finite lens radii from the polytropic M-R relation, validated by recovering the published monochromatic PBH bounds.
- **Hidden-sector phase-transition axion minicluster dark matter** ([arXiv:1609.00208](https://arxiv.org/abs/1609.00208), score 36): No microlensing likelihood exists for extended AU-scale lenses in this scenario; point-MACHO limits are not directly applicable and survey efficiencies were never assembled.
  *Proposed resolution:* Build a likelihood from published EROS/OGLE/HSC-Subaru microlensing survey efficiency functions with finite-size and extended-lens magnification corrections for the predicted (M_m, R_m, f_MC) population.
- **Cosmological gauge field dark matter** ([arXiv:2203.12405](https://arxiv.org/abs/2203.12405), score 36): No likelihood or constraint mapping exists connecting the predicted CGF dark-star mass-radius population to observational compact-object data (microlensing, GW merger catalogs), so the dark-star branch of the route ends without any data comparison.
  *Proposed resolution:* After unit verification, map the TOV mass-radius solutions onto existing compact-object constraint machinery: recast microlensing (OGLE/Subaru-HSC) and LIGO/Virgo subsolar or exotic-compact-object search limits as bounds on the CGF dark-star abundance fraction.
- … and 27 more models (see `gap_roadmap.html`).

### PBH formation & abundance pipeline  —  `tool`
Priority weight 654 across 24 models (89 gap entries overall). Severity: blocks_all_observables × 11, blocks_key_observables × 62, degrades_precision × 15, minor × 1. Effort: medium × 66, small × 17, large × 6.

- **Non-cold fermionic dark matter from primordial black hole evaporation** ([arXiv:2004.14773](https://arxiv.org/abs/2004.14773), score 36): No released pipeline connects the Hawking-emission spectrum and evaporation-history redshifting to the CLASS ncdm f(q) input; the paper's implementation is private, so the transfer-function route cannot be reproduced end to end.
  *Proposed resolution:* Write and release a small converter that takes BlackHawk (or analytic) emission spectra, applies the radiation- vs PBH-dominated redshifting branches, and outputs a CLASS-ready tabulated psd file, validated against the paper's transfer-function fits.
- **PBH sterile neutrinogenesis of sterile neutrino dark matter** ([arXiv:2309.12258](https://arxiv.org/abs/2309.12258), score 35): The abundance calculation assumes a monochromatic PBH mass function and instantaneous evaporation; no implemented calculation handles extended mass functions and time-resolved evaporation for the sterile-neutrino yield and spectrum, which shifts both f_s and the warmness bound.
  *Proposed resolution:* Use or extend a public PBH evaporation code (BlackHawk-style time-dependent greybody emission) coupled to a background Friedmann solver to integrate sterile-neutrino production over extended mass functions and non-instantaneous reheating, outputting f_s and f(p) directly.
- **Halo axion-star QCD axion dark matter** ([arXiv:2404.10340](https://arxiv.org/abs/2404.10340), score 35): No public Schrodinger-Poisson boundary-value solver is configured for halo axion stars, i.e. solitons embedded in an axion minicluster or PBH-seeded minihalo with an external gravitational potential and halo-density boundary conditions, as required to obtain the modified mass-radius relation M_AS(m_a, M_halo, M_PBH).
  *Proposed resolution:* Extend an existing public Schrodinger-Poisson soliton solver (e.g. PyUltraLight or a standalone boundary-value soliton profile code) to include a fixed external PBH point-mass potential plus minicluster halo boundary conditions, and validate against the paper's halo axion-star mass-radius relation.
- … and 21 more models (see `gap_roadmap.html`).

### CMB likelihood & ΔN_eff machinery  —  `likelihood`
Priority weight 644 across 26 models (120 gap entries overall). Severity: blocks_key_observables × 43, degrades_precision × 68, minor × 9. Effort: medium × 89, small × 24, large × 7.

- **Bose-Einstein-condensed scalar field dark matter** ([arXiv:1310.6061](https://arxiv.org/abs/1310.6061), score 39): There is no current-data likelihood for the SFDM expansion-history constraint: the quoted bounds on m and lambda/(mc^2)^2 derive from 2013-era BBN abundances and CMB equality values, and the card flags that recasting is needed before the allowed region can be trusted.
  *Proposed resolution:* Couple the gap_1 background solver to current Planck CMB and PRIMAT/PArthENoPE-style BBN likelihoods in Cobaya, sampling (m, lambda/(mc^2)^2) directly so the N_eff(t) and z_eq shifts are constrained by modern data rather than the historical 68% regions.
- **PBH-evaporation gravitational dark matter from curvature-perturbation PBHs** ([arXiv:2304.09194](https://arxiv.org/abs/2304.09194), score 34): No likelihood or public sensitivity module exists for the MHz-GHz stochastic induced-GW signal, since detectors in this band are only proposed; the route ends at a spectrum with nothing to compare against quantitatively.
  *Proposed resolution:* Assemble published projected sensitivity curves of proposed high-frequency GW detectors (levitated sensors, bulk acoustic wave, Gertsenshtein-conversion searches) into a reusable power-law-integrated sensitivity/likelihood module for Omega_GW(f) in the MHz-GHz band.
- **Stable scalar mediator self-interacting fermion dark matter** ([arXiv:1910.01238](https://arxiv.org/abs/1910.01238), score 32): No reusable likelihood or recast module exists for the CMB, BBN, supernova-cooling, and beam-dump constraints on the visibly-decaying dark Higgs h2 in the (m_h2, sin theta, tau_h2) plane, on which the model's cosmological viability hinges.
  *Proposed resolution:* Digitize the published light-scalar bounds (BBN energy-injection, Planck CMB, SN1987A cooling, beam-dump exclusions) into a constraint module keyed to (m_h2, mixing, lifetime), and combine with DDCalc-style direct-detection limits for the joint allowed-region evaluation.
- … and 23 more models (see `gap_roadmap.html`).

### Milky Way satellite / subhalo-count likelihood  —  `likelihood`
Priority weight 642 across 22 models (23 gap entries overall). Severity: blocks_key_observables × 11, degrades_precision × 12. Effort: medium × 16, large × 7.

- **Heavy-scalar-decay non-thermal warm dark matter** ([arXiv:2305.15736](https://arxiv.org/abs/2305.15736), score 50): The combined Milky Way satellite (DES/Pan-STARRS1) plus strong-lensing flux-ratio likelihood that produces the m_nonthermal >= 107 keV bound is not packaged for direct use with this model's nonthermal transfer functions; only the thermal-equivalent WDM mapping connects the modified-CLASS output to the published limits.
  *Proposed resolution:* Interface the modified-CLASS T(k) output with the public MW satellite likelihood (Nadler et al. DES+PS1 framework) and a lensing flux-ratio substructure likelihood, parameterized by half-mode mass or directly by the nonthermal T(k), so the 107 keV bound can be rerun and updated without the thermal-relic detour.
- **Two-state inelastic self-interacting dark matter** ([arXiv:1805.03203](https://arxiv.org/abs/1805.03203), score 46): No public likelihood exists connecting the model's predictions (density profiles, subhalo Vmax functions, local speed distribution) to data; the card lists only 'profile and subhalo summary-statistic likelihoods; no public full likelihood identified' and the elastic-SIDM constraint set explicitly requires recasting for inelastic scattering.
  *Proposed resolution:* Build an emulator of core sizes, central densities, and subhalo Vmax functions over (sigma/m, Delta/m_chi, f_excited) from the simulation grid and couple it to existing Milky Way satellite and dwarf-kinematics likelihoods, replacing the elastic-equivalent recast factor with a direct inelastic-parameter constraint.
- **Dissipative dark matter from multiple Standard Model sectors** ([arXiv:1804.02847](https://arxiv.org/abs/1804.02847), score 37): No likelihood pipeline maps dissipative-halo predictions onto small-scale-structure data (Milky Way satellite counts, field velocity function, halo structure), so the small_scale_halo_phenomenology comparison remains pass/fail by eye.
  *Proposed resolution:* Adapt existing Milky Way satellite likelihoods (DES plus Pan-STARRS style) and velocity-function datasets to consume the suppressed low-mass halo function and dissipative profiles produced by the steady-state solver or simulations, exposing N_sec and epsilon as likelihood parameters.
- … and 19 more models (see `gap_roadmap.html`).

### Axion / fuzzy-DM field solver  —  `derivation`
Priority weight 578 across 20 models (64 gap entries overall). Severity: blocks_all_observables × 1, blocks_key_observables × 25, degrades_precision × 32, minor × 5, unknown × 1. Effort: medium × 43, large × 8, small × 12, major_program × 1.

- **Decaying warm majoron dark matter** ([arXiv:1803.05650](https://arxiv.org/abs/1803.05650), score 52): The nonthermal majoron phase-space distribution (from misalignment-like production or entropy-diluted scenarios mentioned in the paper) has not been derived, so all structure-formation predictions rest on the thermal-distribution assumption that the card marks as high extrapolation risk.
  *Proposed resolution:* Derive f(q) for the paper's cited nonthermal production channels (freeze-in-like decoupling and entropy-dilution histories) analytically or with a momentum-dependent Boltzmann calculation, and feed the resulting distributions into the extended CLASS ncdm sector in place of the thermal ansatz.
- **Baryon-triality DFSZ axion and light-axino dark matter model** ([arXiv:1807.02530](https://arxiv.org/abs/1807.02530), score 45): The freeze-in axino momentum distribution f(p) from PQ-suppressed weak scattering production is not derived (the card records distribution_function_available: no), so the warm transfer cutoff and mixed-DM matter power cannot be computed beyond rough free-streaming estimates.
  *Proposed resolution:* Solve the unintegrated freeze-in Boltzmann equation for axino production from MSSM scattering with PQ-suppressed couplings, tabulate f(p) over (m_axino, f_a, T_RH), and feed it to CLASS as an ncdm species alongside a cold axion fluid.
- **Inflation-produced dark photon dark matter with Proca-star substructure** ([arXiv:2203.10100](https://arxiv.org/abs/2203.10100), score 43): Tidal survival of solitons, fuzzy envelopes, and low-density compact halos inside galactic hosts is only argued qualitatively (environment dependent), so the present-day substructure abundance entering any gravitational probe is not quantified.
  *Proposed resolution:* Derive tidal-stripping and disruption criteria for Proca-star plus fuzzy-halo profiles (semi-analytically, validated with idealized host-orbit simulations from the extended solver of gap_1) and publish survival fractions versus host mass and orbital radius.
- … and 18 more models (see `gap_roadmap.html`).
